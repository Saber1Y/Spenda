{
  "name": "spenda-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Spenda agent client + off-chain paymaster sponsorship signer",
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
